#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    float jarak;
    float waktu;
    float kecepatan;
    
    scanf("%f", &jarak);
    scanf("%f", &waktu);
    kecepatan = jarak / waktu;
    printf("%.3f", kecepatan);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
